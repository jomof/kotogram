---
trigger: always_on
---

Run test.sh --help to see options (for example, running just hygiene checks, or just a subset of tests)
DO NOT pipe test.sh output. If you do, I can't see what's happening.